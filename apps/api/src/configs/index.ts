/**
 * The configs module's public API.
 *
 * Nothing but the module class: no other module needs a firmware capture, so
 * this module publishes no facade.
 */
export { ConfigsModule } from './configs.module';
