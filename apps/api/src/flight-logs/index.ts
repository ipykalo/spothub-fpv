/**
 * The flight-logs module's public API.
 *
 * Nothing but the module class: nothing else in the API needs to reach the
 * import pipeline.
 */
export { FlightLogsModule } from './flight-logs.module';
