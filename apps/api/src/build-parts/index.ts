/**
 * The build-parts module's public API.
 *
 * Nothing but the module class: no other module needs an install, so this
 * module publishes no facade.
 */
export { BuildPartsModule } from './build-parts.module';
